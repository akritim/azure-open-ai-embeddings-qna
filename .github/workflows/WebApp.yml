name: WebApp Docker Image

on:
  push:
    branches: [ "akriti-patch-1" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    
    - name: Docker Login
      uses: docker/login-action@v2.1.0
      with:
        username: akritimehta12
        # Password or personal access token used to log against the Docker registry
        password: ${{ secrets.DOCKER_TOKEN }}
    
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run:
        docker build . --file WebApp.Dockerfile --tag akritim/oai-embeddings:$(date +'%Y-%m-%d')_$GITHUB_RUN_NUMBER;
        docker tag akritim/oai-embeddings:$(date +'%Y-%m-%d')_$GITHUB_RUN_NUMBER akritim/oai-embeddings:latest;
        docker push akritim/oai-embeddings:$(date +'%Y-%m-%d')_$GITHUB_RUN_NUMBER;
        docker push akritim/oai-embeddings:latest;
        
        docker build . --file BatchProcess.Dockerfile --tag akritim/oai-batch:$(date +'%Y-%m-%d')_$GITHUB_RUN_NUMBER;
        docker tag akritim/oai-batch:$(date +'%Y-%m-%d')_$GITHUB_RUN_NUMBER akritim/oai-batch:latest;
        docker push akritim/oai-batch:$(date +'%Y-%m-%d')_$GITHUB_RUN_NUMBER;
        docker push akritim/oai-batch:latest;
